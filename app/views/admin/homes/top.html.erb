<div class="container mt-3">
  <div class="row justify-content-between mb-4">
    <div class="title col-md-auto px-auto">
      <p class="text-dark bg-light text-center">注文履歴一覧</p>
      </div>
  </div>
  <div class="row">
    <table class="table table-bordered">
    <thead>
      <tr>
      <th scope="col" class="col-2">購入日時</th>
      <th scope="col" class="col-2">購入者</th>
      <th scope="col" class="col-2">注文個数</th>
      <th scope="col" class="col-2">注文ステータス</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
      <tr>
    <td><%= order.created_at %></td>
    <td><%= order.customer.name %></td>
    <td><%#= order_details.amount %></td>
    <td><%#= order_detalis.making_status %></td>
    </tr>
  <% end %>
      </tbody>
    </table>
  </div>
</div>