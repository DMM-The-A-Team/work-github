商品一覧

<%= @items.each do |item| %>
  <% if item.image.attached? %>
    <%= image_tag item.image, size: "40x40" %>
  <% else %>
    <%= image_tag 'no_image', size: "40x40" %>
  <% end %>
    <%= item.name %>
    <%= item.price %>
<% end %>